name: Release

on:
  workflow_dispatch:
    inputs: {}


jobs:
  release:
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: echo Hello from Octo Organization
      - name: Import GPG key
        id: import_gpg
        uses: crazy-max/ghaction-import-gpg@v4
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.PASSPHRASE }}
      - name: Gradle build
        run: |
          ./gradlew clean build -Psigning.keyId=${{secrets.SIGNING_KEY_ID}} -Psigning.password=${{secrets.PASSPHRASE}} -i
      - name: Run a one-line script
        run: echo ls build/libs


#  build-upstream:
#    name: ${{ github.event.client_payload.message }}
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#      - uses: actions/setup-java@v3
#        with:
#          distribution: 'temurin'
#          java-version: '11'
#      - name: Build
#        run: |
#          unset GEM_PATH GEM_HOME JRUBY_OPTS
#          ./ci/test-asciidoctor-upstream.sh ${{ github.event.client_payload.branch }}
